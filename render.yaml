services:
  - type: web
    name: dungeon-delvers-backend
    env: node
    buildCommand: |
      cd backend
      npm install
      npm run build
    startCommand: |
      node dist/server.js
    envVars:
      - key: NODE_VERSION
        value: 18
    autoDeploy: true
    rootDir: .